<!DOCTYPE html>
<html lang="en">

<%-include ('../partials/head') %>


    <%-include('../partials/top-navbar')%>
    <style>
       body {
      margin: 0;
      padding: 0;
    }

    .video-container {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
      z-index: -1;
    }

    .video-container video {
      /* min-width: 100%; */
      max-width: 100%;
      min-height: 100%;
      object-fit: cover;
    }
    
    .card {
      position: relative;
      /* height: 270px;
      width: 450px; */
      border-radius: 25px;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(5px);
      border: 2px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 0 80px rgba(0, 0, 0, 0.2);
      overflow: hidden;
    }</style>

    <body >
      <div class="video-container">
        <video src="assets/imgs/ohyeah.mp4" autoplay muted loop></video>
      </div>
      <div class="container-fluid mx-auto">
        <div class="row mt-5 justify-content-center">
          <div class="col-sm-6 text-black card" style="width: min-content;">
              <span class="text-danger fw-bold fs-5"><%= message %></span>
            <form action="/login" method="POST" style="width: 23rem;">
              <h3 class=" login fw-bold mt-5 mb-3 pb-3  text-center  " style="letter-spacing: 1px;">Log in</h3>

              <div class="mb-3">
                <label for="email" class="form-label">Email address</label>
                <input type="email" class="form-control" id="email" name="email" aria-describedby="emailHelp">
              </div>
              <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control" id="password " name="password">
              </div>
              <p class="mb-2 "><a class="forget" href="/forgot/forgot-p">forgot password?</a></p>
              <div class="mb-4 d-flex justify-content-center  btn-lg">
                <button class=" btn btn-success login-btn m-auto px-5 py-1 rounded-pill fs-3" type="submit" name="submit"
                  id="submit">Login</button>
              </div>

              <p>New to here? <a href="/register" class="linkinfo text-decoration-none fs-4"> Sign up</a></p>
            </form>

          </div>
        </div>
      </div>
      <br>

      <%- include ('../partials/bottom-navbar') %>


    </body>

</html>